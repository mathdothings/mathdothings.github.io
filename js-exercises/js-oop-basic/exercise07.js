String.prototype.subset = function() {
  let subsetStr = [];

  function calcPossibilities(this.length) {

    if (this.length <= 1) return 1;
    else {
      return this.length * calcPossibilities(num - 1);
    }

    for (let x = 0; x < calcPossibilities(this.length); x++) {
      
    }

  return subsetStr;
}
